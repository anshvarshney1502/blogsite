import React from "react";
import LoginClient from "./login-client";

export const metadata = {
  title: "Admin Portal Login",
  description: "Secure access control gate."
};

export default function LoginPage() {
  return <LoginClient />;
}
